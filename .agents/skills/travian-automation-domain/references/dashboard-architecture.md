# Dashboard Architecture Pointer

Canonical project notes live in `docs/dashboard-architecture.md`.

Use that document before changing Livewire dashboard components, row controls, modals, loading states, or activity-log behavior.
